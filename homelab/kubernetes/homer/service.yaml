apiVersion: v1
kind: Service
metadata:
  name: homer
  namespace: homer
spec:
  type: NodePort
  selector:
    app: homer
  ports:
    - name: http
      nodePort: 31120
      port: 8080
      targetPort: 8080
      protocol: TCP
